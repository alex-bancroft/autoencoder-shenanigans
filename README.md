Stuff you'll need (other than requirements.txt):
* huggingface account/CLI access
* if you want to use CUDA, CUDA on the system level and a torch implementation that's compatible (might not get that by default)